# OCR.space API密钥
# 注册地址: https://ocr.space/registration/
# 免费版: 每天500次请求
# PRO版: 支持更多功能和更高的请求限制

OCR_API_KEY=your_api_key_here

# OCR配置
OCR_LANGUAGE=chs  # chs=简体中文, eng=英语
OCR_ENGINE=2      # 1=旧引擎, 2=新引擎(推荐)
OCR_SCALE=true    # 是否放大图片以提高识别率

# 请求配置
REQUEST_TIMEOUT=120    # 单个请求超时时间(秒)
SLEEP_TIME=1.2         # 请求间隔时间(秒),防止限流
MAX_RETRIES=3          # 失败重试次数
